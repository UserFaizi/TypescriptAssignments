function make_shirt(size, message) {
    console.log("You have ordered a ".concat(size, " shirt with the message \"").concat(message, "\" printed on it."));
}
// Call the function with an example size and message
make_shirt("L", "I love TypeScript!");
